# Prompt Log

## 2026-09-23

The window should be the small horizontal shape it started as! the buddy should be on the side of the timer doing its animation and it should not be the main focus becuase the timer is. Also the duplicate drawing problem is still happening. Instead of showing the one singular drawing the user made it shows multiple of the same drawings/animations but smaller! it shouldn't do that! it should only show one!

## 2026-09-23

The window should stay the same small size it has always been but the animation should fit the entire little screen that in resevered for the buddy. Just the small window inside the draggable window. Write this down as one of the breaks in README (the window problems and the sizing problems and the copy drawings problems)

## 2026-09-23

stop making duplicates in the window. it should just be one of the animation not a bunch of copies. one big animation. it should fill up the whole window screen. 16x16 or 100x100 it should only fit one of those animations in the window where the buddy is shown.

## 2026-09-23

There should only be one of the images that the user draws, it should not be animated. When the window opens/buddy spawns, the pop up animation that the user made (even if its nothing, just blank canvas for 100 frames) should play out and then the idle animation plays over and over and when the user closes the window it should play the exit animation and then close. ONLY ONE BUDDY!

## 2026-09-23

It stills makes a bunch of copies when you open the window(the window should be draggable as well). Change the font to something more cheerful. When you pick a color from the color wheel and choose one of the selected slots to replace there should be a button that says "apply color wheel to selected slot".

## 2026-09-23

make the design less corporate and more friendly with softer and more fun but not crazy buttons and better UI. Also there should only be one buddy not multiple when you spawn it.(can you write this as a break in the README). Also the timer should start when you spawn the buddy.

## 2026-09-22 12:00

making a tool. It's a desktop pet called B.U.D.D.Y. (can be changed later). It reminds you of your work every 15 minutes (can add a custom message but comes with a preset reminder). The buddy is fully customizable, you can draw it on a pixel grid and give it a pop up animation, idle animation, and an exit animation. Can you write me a roadmap for this project and make sure to add all my past/present/future prompts into PROMPTS.MD?

## 2026-09-22 13:00

can you make it a broswer for the time being. It should be a simple and friendly website where you can spawn your B.U.D.D.Y. and start the 15 min timer. Below you can draw your buddy on a pixel grid. Fully white background, a brush (b), eraser (e), fill (f), and eyedropper tool (i). 20 custom colors that you can change using a color wheel and pick the first 9 with keys 1-9. The grid should be customizable as well from 16x16, 25x25, 32x32, 50x50, and 100x100. There should be an idle, pop up, and exit animation each with their own customizable frames per second. you should be able to copy frames and delete frames and playtes the animation. When you spawn your buddy it should open a small window with your buddy and the time and do its pop up animation with its greeting message, then its idle animation with occansional messages, then when you close the window it does its exit animation and its exit message. Also for prompts only save the prompt and its time.
